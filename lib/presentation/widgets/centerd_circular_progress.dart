import 'package:flutter/material.dart';

class CircularProgressIndactor extends StatelessWidget {
  const CircularProgressIndactor({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}